import {
  type FC,
} from "react";

const CreateLocation: FC<CreateLocation.Props> = ({}) => {

  return <>
  </>
}

namespace CreateLocation {
  export type Props = Readonly<{
  }>;
}

export {
  CreateLocation as Component,
}